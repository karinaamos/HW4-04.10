#include "stdio.h"

int main() {
    int a,b,c;
    scanf("%d %d %d", &a, &b, &c);
    if (a*a+b*b == c*c){
        printf("Прямоугольный");
    }
    if (a*a+b*b > c*c){
        printf("Тупоугольный");
    }
    if (a*a+b*b < c*c){
        printf("Остроугольный");
    }
    return 0;
}
